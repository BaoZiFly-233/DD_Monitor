# Overhaul V3 回归测试矩阵

## 1. 基础启动与编译

- [x] `python -m py_compile "LiverSelect.py" "VideoWidget_mpv.py" "danmaku_renderer.py" "danmaku_layout.py" "DD监控室.py"`
- [x] 冷启动进入主界面（无崩溃、无弹窗异常）
- [x] 登录后自动拉关注列表成功

## 2. 列表与卡片行为

- [x] 新增直播间后卡片在 1s 内刷新状态
- [x] 删除直播间后布局立即重排，无“空洞”占位
- [x] 置顶/取消置顶后顺序立即更新
- [x] 短号房间解析为长号后，后续刷新/置顶行为正常

## 3. 流地址与播放兜底

- [x] 直播中房间可正常播放首个候选流
- [x] 首个候选失败时自动切换到下一个候选
- [x] 日志不再出现 `Invalid URL ''`
- [x] 房间下播/加密时 UI 状态正确

## 4. 弹幕体验

- [x] 滚动弹幕连续显示，无明显闪烁
- [x] 顶部弹幕默认开启并可显示
- [x] 底部弹幕默认开启且位置不过低
- [x] 关闭顶部/底部后立即生效

## 5. 日志与稳定性

- [x] `unknown cmd` 日志不再输出巨量 payload
- [x] 同一未知 cmd 在 5 分钟内最多一条 warning
- [x] `-352` 出现时热门列表可自动 fallback

## 6. 性能抽样（建议）

- [x] 16 窗口 + 弹幕开启：记录 CPU 平均占用
- [x] 32 窗口 + 弹幕开启：记录 CPU 峰值与 UI 卡顿感受
- [x] 卡片新增/删除操作：记录 UI 响应延迟

## 7. 结果记录模板

| 场景 | 结果 | 备注 |
|------|------|------|
| 基础启动 |  |  |
| 列表新增/删除 |  |  |
| 置顶切换 |  |  |
| 流候选回退 |  |  |
| 弹幕顶部/底部 |  |  |
| unknown cmd 日志 |  |  |
| 16 窗口性能 |  |  |
| 32 窗口性能 |  |  |

## 8. UI 交互补强（2026-03-07）

- [x] 代码修复：左键拖拽仅在“本窗口内按下 + 移动超阈值”后触发
- [x] 代码修复：未播放状态停止 mpv 渲染，空窗口改为灰底
- [x] 代码修复：控制栏交互添加按压保活，避免点击后立刻消失
- [ ] 实机验证：切回软件时不再误进入拖拽状态
- [ ] 实机验证：未播放窗口显示灰底而非黑底
- [ ] 实机验证：关闭弹幕后底栏可连续点击切换，无需鼠标移出再移入
